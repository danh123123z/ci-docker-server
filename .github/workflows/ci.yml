name: CI Docker Server

on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  long-workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 6 tiếng
    steps:
      - uses: actions/checkout@v4

      - name: Start long CI job
        run: |
          echo "Running CI job for 6 hours..."
          sleep 21600
